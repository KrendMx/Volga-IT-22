import React, {useState, useEffect} from 'react';
import {Headers} from "../../styled"
const Header = (props) => {
    return (
        <Headers>
            
        </Headers>
    );
};

export default Header;
