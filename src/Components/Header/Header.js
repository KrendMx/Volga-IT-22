import React, {useState, useEffect} from 'react';
import {Header} from "../../styled"
const Headers = (props) => {
    return (
        <Header>
            
        </Header>
    );
};

export default Headers;
